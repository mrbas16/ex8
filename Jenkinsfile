pipeline {
    agent any

    stages {
        stage('Clone repo') {
            steps {
                git 'https://github.com/mrbas16/ex8.git'
                bat 'dir'
            }
        }
    }
}
